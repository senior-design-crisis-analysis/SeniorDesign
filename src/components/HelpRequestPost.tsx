import { useEffect, useRef } from "react";
//import supabase from "../supabase-client";

const HelpRequestPost = () => {

  return (
    <div>
        <h1>HelpRequestPost</h1>
    </div>
  );
};

export default HelpRequestPost;